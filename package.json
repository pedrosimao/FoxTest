{
  "name": "mail-scraper",
  "version": "1.0.0",
  "description": "scrape information from SNCF e-mail",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "npm run build && node index.js"
  },
  "author": "Pedro Simao / Seve",
  "license": "ISC",
  "dependencies": {
    "accounting": "^0.4.1",
    "axios": "^0.21.4",
    "cheerio": "^1.0.0-rc.2",
    "chrono-node": "^1.3.5",
    "fs": "0.0.1-security",
    "rimraf": "^2.6.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
